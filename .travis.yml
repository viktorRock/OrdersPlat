language: node_js
node_js: "6.10.0"
install:
  - echo "installing"
  - npm install -g firebase-tools
  - npm install
script:
  - echo "Scripting"  
after_sucess:
  - echo "Success"
  - echo $TRAVIS_TEST_RESULT
  - firebase deploy --token $FIREBASE_TOKEN
after_failure:
  - echo "Failure"
  - echo $TRAVIS_TEST_RESULT
